// js is use for adding ineractivity in html

// terminal spinup

// window is a object

// window.document ---> reffer the whole html tree

// how to add back filter backdropfilter

document.write("Hello from js");

button.addEventListener("click", () => {
  document.querySelector("#body").style.background = "white";
});

function changeColor(color) {
  document.body.style.backgroundColor = color;
}

let darkmode = document.querySelector("#darkmode");

// on-of is scalled toggle in usa
// depricate means funnctionality is no more in future
// refactor menas renamming
// rendering means when a website is view first time
