Function.prototype.describe = function () {
  console.log(`Function name is ${this.name}`);
};

Function.prototype.name = "Rohan";
function one() {
  let name = "irfan";
}

function add(a, b) {} // function declaretion

const substract = funnction(a, b); // function expression

// First class function or higher order function

function createCounter() {
  let;
}

// Tiffin concept, a function that use parrent function variables also valid in return is clouser

// Immediately Invoked Function Expression (IIFE)

// Function curry

// Lexical Analusys

// usestate // use effect
